<%= form_for(@survey) do |f| %>
<% if @survey.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this team from being saved:</h2>

    <ul>
    <% @survey.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%#<div class="form-group">
  <%= collection_check_boxes(:survey, :question_id, Question.all, :id, :question_text) %>
<%#</div>%>

<div class="form-group">
      <%= collection_check_boxes :survey, :question_ids, Question.all, :id, :question_text %>
    </div>

<div class="actions">
  <%= f.submit class: "btn btn-success" %>
</div>
<% end %>
